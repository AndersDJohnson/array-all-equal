# array-all-equal

Check if all of an array's items are equal.

```js
import arrayAllEqual from 'array-all-equal'

arrayAllEqual(['a', 'a', 'a']) // true
arrayAllEqual(['a', 'b', 'a']) // false
arrayAllEqual([]) // true
```

